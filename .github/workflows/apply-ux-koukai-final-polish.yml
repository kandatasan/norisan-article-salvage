name: Apply UX koukai final prose polish

on:
  push:
    branches:
      - main
    paths:
      - scripts/apply_ux_koukai_final_polish.py
      - .github/workflows/apply-ux-koukai-final-polish.yml

permissions:
  contents: read
  issues: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Compile final polish script
        run: python -m py_compile scripts/apply_ux_koukai_final_polish.py
      - name: Apply guarded UX final prose polish
        env:
          TSURIKUE_WP_USER: ${{ secrets.TSURIKUE_WP_USER }}
          TSURIKUE_WP_APP_PASSWORD: ${{ secrets.TSURIKUE_WP_APP_PASSWORD }}
        run: python scripts/apply_ux_koukai_final_polish.py
      - name: Record audit result
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          {
            echo '### ux-koukai final prose polish'
            echo
            if [ -f reports/ux-koukai-final-polish/summary.md ]; then
              cat reports/ux-koukai-final-polish/summary.md
            else
              echo '- result: **BLOCKED_BEFORE_APPLY**'
              echo '- wordpress_write_count: **0**'
            fi
          } > /tmp/comment.md
          gh issue comment 22 --repo "$GITHUB_REPOSITORY" --body-file /tmp/comment.md
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ux-koukai-final-polish
          path: reports/ux-koukai-final-polish/
          if-no-files-found: warn
