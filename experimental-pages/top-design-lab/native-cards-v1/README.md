# Homepage native image cards v1

Stable homepage category cards using native Gutenberg Image blocks with direct links. The live deploy script replaces only the four legacy Cover cards at the known homepage snapshot, updates old category-list links to the dedicated hub pages, and verifies desktop/mobile rendering before keeping the change.
