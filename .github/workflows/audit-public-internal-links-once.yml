name: Audit public internal links once

# Re-run after the 2026-08-22 public internal-link optimization.
on:
  push:
    branches: [main]
    paths:
      - 'scripts/audit_public_internal_links_once.py'
      - 'scripts/identify_recent_gopro_media_once.py'
      - '.github/workflows/audit-public-internal-links-once.yml'

permissions:
  contents: read
  issues: write

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Audit current published internal links (GET only)
        env:
          TSURIKUE_WP_USER: ${{ secrets.TSURIKUE_WP_USER }}
          TSURIKUE_WP_APP_PASSWORD: ${{ secrets.TSURIKUE_WP_APP_PASSWORD }}
        run: python scripts/audit_public_internal_links_once.py
      - name: Identify recent GoPro media (GET only)
        env:
          TSURIKUE_WP_USER: ${{ secrets.TSURIKUE_WP_USER }}
          TSURIKUE_WP_APP_PASSWORD: ${{ secrets.TSURIKUE_WP_APP_PASSWORD }}
        run: python scripts/identify_recent_gopro_media_once.py
      - name: Record audit on issue 22
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          {
            echo '### Public internal-link audit'
            echo
            if [[ -f reports/public-internal-links-audit/summary.md ]]; then
              cat reports/public-internal-links-audit/summary.md
            else
              echo 'No internal-link summary file was produced.'
            fi
            echo
            echo '### GoPro media identification'
            echo
            if [[ -f reports/gopro-media-identify/summary.md ]]; then
              cat reports/gopro-media-identify/summary.md
            else
              echo 'No GoPro media summary file was produced.'
            fi
          } > /tmp/comment.md
          gh issue comment 22 --repo "$GITHUB_REPOSITORY" --body-file /tmp/comment.md
      - name: Upload audit artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: public-audit-and-gopro-media-identify
          path: |
            reports/public-internal-links-audit/
            reports/gopro-media-identify/
          if-no-files-found: error
