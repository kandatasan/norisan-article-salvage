name: Tsurikue homepage category alt apply once

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/tsurikue-home-category-alt-apply-once.yml"

jobs:
  apply:
    if: ${{ github.head_ref == 'homepage-category-alt-20260711' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    env:
      OUTPUT_DIR: reports/tsurikue-home-category-alt-apply

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Run tests
        run: python -m unittest tests/test_tsurikue_home_category_alt.py -v

      - name: Apply approved homepage category alt changes
        env:
          TSURIKUE_WP_USER: ${{ secrets.TSURIKUE_WP_USER }}
          TSURIKUE_WP_APP_PASSWORD: ${{ secrets.TSURIKUE_WP_APP_PASSWORD }}
        run: >-
          python scripts/tsurikue_home_category_alt.py
          --mode apply
          --site-url https://tsurikue.com
          --output-dir "$OUTPUT_DIR"
          --apply-confirmation "APPLY-HOME-CATEGORY-ALT-20260711"

      - name: Upload apply artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tsurikue-home-category-alt-apply
          path: ${{ env.OUTPUT_DIR }}/
          if-no-files-found: warn
