name: Apply Hiroshima bokujyou reader-first rewrite once

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_hiroshima_bokujyou_reader_first_once.py'
      - '.github/workflows/apply-hiroshima-bokujyou-reader-first-once.yml'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  rewrite:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Syntax check
        run: python -m py_compile scripts/apply_hiroshima_bokujyou_reader_first_once.py
      - name: Apply guarded reader-first rewrite
        env:
          TSURIKUE_WP_USER: ${{ secrets.TSURIKUE_WP_USER }}
          TSURIKUE_WP_APP_PASSWORD: ${{ secrets.TSURIKUE_WP_APP_PASSWORD }}
        run: python scripts/apply_hiroshima_bokujyou_reader_first_once.py
      - name: Record result on salvage audit issue
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          {
            echo '### Hiroshima bokujyou reader-first rewrite'
            echo
            cat reports/hiroshima-bokujyou-reader-first/summary.md
          } > /tmp/hiroshima-bokujyou-comment.md
          gh issue comment 22 --repo "$GITHUB_REPOSITORY" --body-file /tmp/hiroshima-bokujyou-comment.md
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hiroshima-bokujyou-reader-first
          path: reports/hiroshima-bokujyou-reader-first/
          if-no-files-found: warn
